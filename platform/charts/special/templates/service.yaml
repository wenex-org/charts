apiVersion: v1
kind: Service
metadata:
  name: {{ include "special.fullname" . }}
  labels:
    {{- include "special.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  {{- with .Values.service.ports }}
  ports:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    {{- include "special.selectorLabels" . | nindent 4 }}
