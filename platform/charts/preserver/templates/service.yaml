apiVersion: v1
kind: Service
metadata:
  name: {{ include "preserver.fullname" . }}
  labels:
    {{- include "preserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  {{- with .Values.service.ports }}
  ports:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    {{- include "preserver.selectorLabels" . | nindent 4 }}
