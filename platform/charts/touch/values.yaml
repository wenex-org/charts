enabled: false

replicaCount: 3

fullnameOverride: "platform-touch"

serviceAccount:
  create: true
  automount: true

service:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3100
      protocol: TCP
      name: http
    - port: 8080
      targetPort: 5100
      protocol: TCP
      name: grpc

ingress:
  enabled: false

autoscaling:
  enabled: false
