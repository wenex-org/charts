apiVersion: v1
kind: Service
metadata:
  name: {{ include "touch.fullname" . }}
  labels:
    {{- include "touch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  {{- with .Values.service.ports }}
  ports:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    {{- include "touch.selectorLabels" . | nindent 4 }}
