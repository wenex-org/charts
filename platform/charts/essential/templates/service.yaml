apiVersion: v1
kind: Service
metadata:
  name: {{ include "essential.fullname" . }}
  labels:
    {{- include "essential.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  {{- with .Values.service.ports }}
  ports:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    {{- include "essential.selectorLabels" . | nindent 4 }}
