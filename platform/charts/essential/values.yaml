enabled: false

replicaCount: 3

fullnameOverride: "platform-essential"

serviceAccount:
  create: true
  automount: true

service:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3050
      protocol: TCP
      name: http
    - port: 8080
      targetPort: 5050
      protocol: TCP
      name: grpc

ingress:
  enabled: false

autoscaling:
  enabled: false
