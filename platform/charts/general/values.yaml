enabled: false

replicaCount: 3

fullnameOverride: "platform-general"

serviceAccount:
  create: true
  automount: true

service:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3070
      protocol: TCP
      name: http
    - port: 8080
      targetPort: 5070
      protocol: TCP
      name: grpc

ingress:
  enabled: false

autoscaling:
  enabled: false
