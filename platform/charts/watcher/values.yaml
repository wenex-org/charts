enabled: false

replicaCount: 3

fullnameOverride: "platform-watcher"

serviceAccount:
  create: true
  automount: true

service:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 4040
      protocol: TCP
      name: http
    - port: 8080
      targetPort: 6040
      protocol: TCP
      name: grpc

ingress:
  enabled: false

autoscaling:
  enabled: false
