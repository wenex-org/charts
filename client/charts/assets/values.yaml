enabled: false

replicaCount: 3

fullnameOverride: "client-assets"

serviceAccount:
  create: true
  automount: true

service:
  type: ClusterIP
  port: 80
  targetPort: 8099

ingress:
  enabled: false

autoscaling:
  enabled: false
