enabled: false

replicaCount: 3

fullnameOverride: "client-services"

serviceAccount:
  create: true
  automount: true

service:
  type: ClusterIP
  port: 80
  targetPort: 7010

ingress:
  enabled: false

autoscaling:
  enabled: false
