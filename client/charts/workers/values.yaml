enabled: false

replicaCount: 3

fullnameOverride: "client-workers"

serviceAccount:
  create: true
  automount: true

service:
  type: ClusterIP
  port: 80
  targetPort: 8010

ingress:
  enabled: false

autoscaling:
  enabled: false
