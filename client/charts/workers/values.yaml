enabled: false

replicaCount: 2

fullnameOverride: "client-workers"

serviceAccount:
  create: true
  automount: true

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false

autoscaling:
  enabled: false
